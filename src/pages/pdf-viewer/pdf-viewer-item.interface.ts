/**
 * An interface for items being viewed in the PDF viewer
 */
export interface PdfViewerItem {
  /**
   * The title of the PDF
   */
  title: string;
  /**
   * The URL of the PDF
   */
  url: string;
}
